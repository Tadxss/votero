# Votero — Architecture & MVP Plan

## Context

The goal is one cross-platform app (iOS + Android + Web, single monorepo, shared business logic) — built as a portfolio/demo piece that's also plausibly monetizable. Votero is a **QR-code-based group voting/polling app**: a creator makes a "lobby" with a question and options, shares a QR code / link, and people scan it to vote — no account required to vote, with optional accounts for creators to keep history.

This concept was picked specifically because it exercises every distinctive part of the stack better than the alternatives considered: Supabase **Realtime** is the headline feature (live join/vote progress, instant open→closed transitions), **anonymous auth** fits "vote without an account" naturally, **RLS** has a genuinely hard problem to solve (hiding voter identity in anonymous-ballot mode), and **Edge Functions** are needed for real reasons (atomic cap enforcement, atomic vote-casting + auto-close), not just as a stack checkbox. Monetization precedent exists (Slido/Mentimeter/StrawPoll: free tier + paid creator tier), and "embed this on any website later" falls out of the URL design almost for free.

**Terminology note**: "unanimous vs not unanimous" voting (product owner's original phrasing) means **anonymous vs attributed (open) ballot mode** — i.e. whether the creator can see who voted for what, not a consensus/decision rule. Code/schema use `ballot_mode: 'anonymous' | 'open'`.

### Confirmed MVP scope
- Public lobbies only for MVP: creator sets a numeric voter cap; first N people to scan/join get to vote. (Private invite-by-username rosters are a deferred feature — schema must not preclude it.)
- Ballot mode is creator-configurable per lobby: **anonymous** (creator sees only aggregate results) or **open** (creator sees who voted for what).
- Tally visibility is creator-configurable per lobby: **live** (everyone sees counts update in real time) or **hidden** (only "X/Y voted" progress until the creator closes the lobby).
- Lobby lifecycle: draft → open → closed. Creator opens/closes manually; auto-closes when the cap is reached and everyone who joined has voted.
- No-account voting via Supabase anonymous auth sessions, persisted per device; one-vote-per-person enforced best-effort via that session (industry-standard, deliberately not solved with OTP for MVP — but the schema reserves a column so a per-lobby OTP toggle can be added later without a rewrite).
- Optional real accounts: creators get persistent lobby history. **Decided during Build Order step 10**: no anonymous→permanent account upgrade path — sign-in only affects what's created *afterward*; a lobby created anonymously stays anonymous forever. Email-confirmation-based linking can silently break across browser contexts, and it's not worth that fragility for what it buys.
- QR encodes a plain `https://` URL (not a custom scheme) so voting works with zero app install — this is deliberate, since random QR-scanners must not be forced to install anything. The same URL is set up (not built yet) to later support universal/app-link interception by the native app, and later still, iframe embedding on third-party sites. A secondary "join by typing the 8-character code" path exists too (home page) for anyone who can't scan (code read aloud, shared as text, camera unavailable) — the same entry point the mobile app will offer once built, since typing a code is the natural fallback there as well.
- Deferred but explicitly *not precluded* architecturally: private invite rosters, per-lobby OTP verification, embeddable third-party widget, a shared `packages/ui` component library (share logic first, unify UI later if it makes sense).

---

## Architecture

### Postgres schema
```sql
create type lobby_status as enum ('draft', 'open', 'closed');
create type ballot_mode as enum ('anonymous', 'open');
create type tally_visibility as enum ('live', 'hidden');
create type lobby_visibility as enum ('public', 'private'); -- only 'public' used in MVP

create table public.profiles (
  id           uuid primary key references auth.users(id) on delete cascade,
  display_name text,
  is_anonymous boolean not null default true,
  created_at   timestamptz not null default now()
); -- populated by an `on auth.users insert` trigger

create table public.lobbies (
  id               uuid primary key default gen_random_uuid(),
  code             text unique not null,             -- short QR/link code, see below
  creator_id       uuid not null references auth.users(id) on delete cascade,
  title            text not null check (char_length(title) between 1 and 200),
  status           lobby_status not null default 'draft',
  ballot_mode      ballot_mode not null default 'anonymous',
  tally_visibility tally_visibility not null default 'hidden',
  visibility       lobby_visibility not null default 'public',  -- future private-lobby hook
  voter_cap        integer not null check (voter_cap > 0 and voter_cap <= 10000),
  joined_count     integer not null default 0,        -- denormalized, maintained only by rpc_join_lobby
  votes_count      integer not null default 0,         -- denormalized, maintained only by rpc_cast_vote
  otp_required     boolean not null default false,     -- future per-lobby stronger-verification hook, unused in MVP
  opened_at        timestamptz,
  closed_at        timestamptz,
  created_at       timestamptz not null default now(),
  updated_at       timestamptz not null default now()
);

create table public.options (
  id       uuid primary key default gen_random_uuid(),
  lobby_id uuid not null references public.lobbies(id) on delete cascade,
  label    text not null check (char_length(label) between 1 and 200),
  position integer not null,
  unique (lobby_id, position)
);

create table public.participants (
  id           uuid primary key default gen_random_uuid(),
  lobby_id     uuid not null references public.lobbies(id) on delete cascade,
  user_id      uuid not null references auth.users(id) on delete cascade,
  display_name text,                        -- shown in open-ballot detail only
  has_voted    boolean not null default false,
  joined_at    timestamptz not null default now(),
  unique (lobby_id, user_id),
  unique (lobby_id, id)                      -- lets votes FK-compose (lobby_id, participant_id)
);

create table public.votes (
  id             uuid primary key default gen_random_uuid(),
  lobby_id       uuid not null references public.lobbies(id) on delete cascade,
  participant_id uuid not null,
  option_id      uuid not null references public.options(id) on delete cascade,
  created_at     timestamptz not null default now(),
  unique (lobby_id, participant_id),          -- hard one-vote-per-person guarantee
  foreign key (lobby_id, participant_id) references public.participants(lobby_id, id) on delete cascade
);
```

Key points:
- `code` (8-char Crockford base32, ambiguous chars excluded, collision-retried trigger default) is the only public lobby identifier — never expose the uuid `id` for lookup.
- `joined_count`/`votes_count` are denormalized onto `lobbies` specifically so a single Realtime subscription on that one row drives all progress UI (see Realtime below).
- Anonymous vs. open ballot is a property of the **lobby**, never a flag on the vote row — visibility is enforced entirely by which server function you're allowed to call, not by data shape.
- `visibility` and `otp_required` columns exist now, unused in MVP, specifically so private-roster and stronger-verification can land later as additive changes, not migrations that touch the hot path.

### RLS policy design
Enable RLS everywhere. **`votes` gets zero client SELECT/INSERT policies at all — default deny.** RLS can filter rows but can't express "you may see an aggregate count but not the individual row," and any policy permissive enough to compute a client-side tally is also permissive enough to leak the exact voter→option linkage anonymous mode must hide. So:
- `lobbies`: creator sees their own (any status); everyone else sees `public` + non-`draft` lobbies.
- `options`: readable wherever the parent lobby is readable; writable by creator only while `status = 'draft'`.
- `participants`: creator can see the full roster (names + `has_voted` boolean) for their own lobby — safe, since knowing *who joined* and *whether* they voted never reveals *what* they voted for; a participant can see their own row. No client INSERT policy — joining goes through `rpc_join_lobby` only.
- `votes`: no policies at all. All access (insert, tally, ballot detail) goes through `SECURITY DEFINER` RPCs below.

**RLS is not enough on its own — table/function GRANTs are required too.** This Supabase version's default is `auto_expose_new_tables` off: new tables are **not** automatically reachable by `anon`/`authenticated` the way older Supabase docs/tutorials assume. A GRANT is what lets a role attempt an operation at all; RLS then further restricts *which rows* that operation can see/touch. Without the GRANT, every query fails with "permission denied for table X" before RLS is ever evaluated. `supabase/migrations/..._grants.sql` grants exactly: `select, insert, update` on `lobbies`, `select, insert` on `options`, `select` on `participants` and `profiles` — and nothing at all on `votes` (the strongest possible lockdown: no policy *and* no grant). The same rule applies to functions invoked transitively from an invoker-mode call — see `generate_lobby_code`'s grant in that migration.

### Edge Functions (Deno) wrapping `SECURITY DEFINER` Postgres RPCs
Thin Edge Functions validate input/auth and call Postgres functions that do the atomic transactional work (locking primitives live in Postgres, not Deno).

- **`rpc_create_lobby`** — plain RPC, no true Edge Function needed (single-statement insert of lobby+options, no race, nothing sensitive to hide). Called out to contrast with the three below.
- **`join-lobby` → `rpc_join_lobby(code, display_name)`** — needed because cap enforcement ("read joined_count, compare to cap, conditionally insert") races under concurrent scans at the boundary; the RPC does `select ... for update` on the lobby row to serialize joiners, checks for an idempotent re-join, then inserts + increments atomically.
- **`cast-vote` → `rpc_cast_vote(lobby_id, option_id)`** — needed because one vote touches four things atomically (insert vote row, flip `participants.has_voted`, increment `lobbies.votes_count`, conditionally flip `status → closed`), and auto-close ("cap reached AND all joined have voted") must be checked inside the same transaction as the increment to avoid racing a manual close. `select ... for update` on the lobby row serializes this per-lobby.
- **`lobby-results` → `rpc_get_tally` (pure aggregate, no identity, always safe) + `rpc_get_ballot_detail` (participant→option linkage, internally gated to `ballot_mode='open' AND creator_id=auth.uid()`, else `FORBIDDEN`)** — two separate function shapes so the identity-carrying shape structurally cannot be requested except by the one path allowed to see it. The Edge Function composes: progress (always safe) + tally (if `tally_visibility='live'` or lobby closed or caller is creator) + ballot detail (only if open-mode creator).
- **`set-lobby-status`** — creator-only open/close, routed through a function rather than a raw client UPDATE for auditability and to centralize the auto-close-vs-manual-close interaction.

### Realtime design
Two primitives for two sensitivity levels:
- **Postgres Changes on `lobbies` only** (`alter publication supabase_realtime add table public.lobbies`) — delivers `status`/`joined_count`/`votes_count` changes to any client the existing `lobbies` SELECT policies already allow. Drives "X/Y joined," "X/Y voted," and the draft→open→closed transition. **`participants` and `votes` are never added to the realtime publication** — replicating `votes` would leak the exact voter→option linkage via the replication payload itself, regardless of any RLS policy.
- **Broadcast channel `lobby:{id}:tally`** — published only by the server side of `cast-vote`, after it has computed `rpc_get_tally`, with a payload of `{counts: [...]}` only — never a participant id. Clients only subscribe when `tally_visibility='live'`. Recommend Realtime Authorization on this topic (gate subscription to callers who can currently SELECT that lobby row) as defense in depth.

### QR / URL / deep-link design
- QR encodes `https://vote.<domain>/vote/{code}` — always a plain https URL, never a custom scheme, so any camera app opens it directly with zero install.
- `apps/web/app/vote/[code]/page.tsx` branches on lobby status: draft → "not open yet"; open+unjoined → bootstrap anonymous auth session + `join-lobby` + show ballot; open+voted → progress/live tally; closed → results (+ ballot detail if open-mode creator).
- Universal/App Links (wired later, not now): because the primary link is always plain https, registering `ios.associatedDomains` / Android App Links intent filters later makes the *same* link open the native app when installed and fall through to the browser otherwise — no architecture change, just two static `.well-known` files plus a mirrored Expo Router route.
- Embeddable widget (deferred): `/vote/[code]` is self-contained (bootstraps its own session, no assumption of being the top window), so a later `?embed=1` iframe mode needs no schema/RLS/Edge Function changes.

---

## Build Order

1. **Scaffold monorepo** — Turborepo, `apps/mobile` (`create-expo-app`, Expo Router), `apps/web` (`create-next-app`, App Router), empty `packages/shared` + `packages/types`, root `turbo.json`/`tsconfig.base.json`, NativeWind configured in both apps. ✅ *done.*
2. **Supabase schema**: `supabase init`; `supabase/migrations/..._init.sql` (enums/tables above), `..._rls.sql`, `..._functions.sql` (all RPCs above + `generate_lobby_code`), `..._realtime.sql` (publication + authorization policy), `..._grants.sql` (explicit table/function GRANTs — see below). ✅ *done and verified against a real local Postgres* (Docker Desktop installed, `supabase start`). Three real bugs were found and fixed by that verification, all worth knowing about: (1) this Supabase version's `auto_expose_new_tables` default is off, so tables are **not** auto-granted to `anon`/`authenticated` anymore — RLS policies only restrict rows on top of an existing GRANT, they don't substitute for one, so a `..._grants.sql` migration explicitly grants exactly what each role needs; (2) the same applies to functions called transitively from an invoker-mode RPC (`generate_lobby_code`, called from `rpc_create_lobby`'s trigger, needed its own grant); (3) a PL/pgSQL variable named `code` in `generate_lobby_code()` collided with the `lobbies.code` column, requiring a rename to `v_code`. Full lifecycle smoke-tested directly via psql (cap enforcement, one-vote-per-person, auto-close, `rpc_get_tally` correctly denied to `authenticated` and only usable via `service_role`, `rpc_get_ballot_detail` correctly `FORBIDDEN` for non-creators *and* for the creator on an anonymous-mode lobby, `votes` table completely unreachable directly).
3. **Edge Functions**: `supabase/functions/{join-lobby,cast-vote,lobby-results,set-lobby-status}/index.ts`, using the `@supabase/server` `withSupabase({ auth: 'user' })` wrapper (current Supabase CLI template convention) — `ctx.supabase` is caller-scoped (RLS/grants apply, correct `auth.uid()`), `ctx.supabaseAdmin` is service-role (used only for `rpc_get_tally`, per its function-level comment). ✅ *done and verified end-to-end* against the local stack via real anonymous-auth sessions (signup → create lobby → open → join → vote → results), including confirming `lobby-results` correctly hides `ballotDetail` from a non-creator caller while still returning the live tally to everyone.

   **Hosted project**: linked to the real Supabase project (`supabase link`), all 5 migrations pushed (`supabase db push`), all 4 Edge Functions deployed (`supabase functions deploy`), and Auth config synced (`supabase config push` — this is how `enable_anonymous_sign_ins` and other `[auth]` settings in `config.toml` actually reach a hosted project; it's a separate step from migrations). The full end-to-end flow above was re-run against the hosted project directly (not just local) with identical results. One config gotcha hit along the way: `config.toml`'s default `[storage.vector] enabled = true` makes `config push` fail with a 402 on any project not on a paid plan — set to `false` since this app doesn't use Storage. Local dev env files (`apps/web/.env.local`, `apps/mobile/.env`, both gitignored) point at the **local** stack (`http://127.0.0.1:54321`) by default; switch them to the hosted project's URL/anon key for staging-like testing.
4. **packages/types**: hand-written `domain.ts` for the enums/DTOs above ✅ *done*; `database.ts` ✅ *done — real generated types*, produced via a `docker run` of the `postgres-meta` image directly (see the file's header comment for the exact command and why: `supabase gen types typescript --local` on this machine's CLI version shells out to `podman` unconditionally rather than the configured Docker runtime, and fails).
5. **packages/shared**: Supabase client with a platform-pluggable storage adapter (RN AsyncStorage vs web localStorage), TanStack Query hooks (`useLobby`, `useCreateLobby`, `useJoinLobby`, `useCastVote`, `useLobbyResults`, `useSetLobbyStatus`, `useLobbyRealtime`). ✅ *done.* (A small Zustand store for in-progress ballot selection UI state existed here too, but was retired in Build Order step 27 — see that step for why.)
6. **Web creator flow**: `apps/web/app/create/page.tsx` (form → `rpc_create_lobby`), `apps/web/app/lobby/[code]/manage/page.tsx` (QR via `qrcode.react`, open/close, live progress/results). ✅ *done and verified via a scripted Playwright browser run (see docs/TESTING.md).*
7. **Web voter flow**: `apps/web/app/vote/[code]/page.tsx` per the status-branch behavior above. ✅ *done and verified* — including the auto-join-on-mount behavior, `hasVoted`-aware refresh handling, and the `LOBBY_FULL` error path.
8. **Mobile creator + voter flow**: Expo Router equivalents of steps 6–7, `react-native-qrcode-svg` for QR display, same `packages/shared` hooks — deliberately not sharing UI components yet (share logic first). *Not started.*
9. **Realtime wiring verification**: confirm the `lobbies` Postgres Changes subscription end-to-end on both apps; confirm the `lobby:{id}:tally` broadcast round-trip from `cast-vote` to a live-tally UI. ✅ *done for web* — verified live "X/Y joined" updates and the live-tally broadcast round-trip with two independent anonymous sessions in separate browser contexts. Mobile side still blocked on step 8.

   **Bugs found building the web UI** (all fixed): (1) two different pnpm-resolved React instances (`apps/web` vs. `packages/shared`, pulled apart by `apps/mobile`'s exact React pin) silently broke React Context — `QueryClientProvider`'s context wasn't visible to hooks running under the other React copy. Fixed with a root `pnpm.overrides` pinning `react`/`react-dom` to one version workspace-wide; this would have hit the mobile build too once it used these hooks with a provider. (2) `useLobby`'s query fired before `useEnsureSession` had a session, hitting `lobbies` as unauthenticated `anon` (which has no grant) and 401ing — masked by TanStack Query's default retry. Fixed by giving `useLobby` an `enabled` option and gating it on session-readiness in both pages. (3) `rpc_join_lobby` never returned `has_voted`, so a page refresh after voting would show the ballot again — added a migration (`..._join_result_has_voted.sql`) to include it.
10. **Accounts/history**: ✅ *done*, with one deliberate change from the original sketch — no anonymous→permanent upgrade. Creators sign in with an email OTP code (`supabase.auth.signInWithOtp`/`verifyOtp`) *before* creating a lobby to have it tied to a real account; a lobby created anonymously stays anonymous forever, there's no retroactive "claim my past anonymous lobbies" flow (email-confirmation-based linking can silently break across browser contexts — not worth it for what it buys). New `packages/shared` hooks: `useAuthUser`, `useSignInWithOtp`, `useVerifyOtp`, `useSignOut`, `useMyLobbies`. New pages: `/login` (two-step email → code), `/lobbies` ("My Lobbies", gated on `useAuthUser().isSignedIn`, not on lobby creation), a persistent `Header` component. `/create` keeps working fully anonymously — the only addition is a one-line, ignorable "sign in to save this to your history" nudge.
    - **Real bug found deploying this**: Supabase's free tier refuses to customize an auth email template while using its default (shared) email sender — `supabase config push` failed with a 400 until a custom SMTP provider was configured. Fixed by wiring up Resend (free tier) via the Management API (`PATCH /v1/projects/{ref}/config/auth`, fields `smtp_host`/`smtp_port`/`smtp_user`/`smtp_pass`/`smtp_admin_email`/`smtp_sender_name`), sending from Resend's shared `onboarding@resend.dev` address (no custom domain verified yet). The custom OTP-code email template (`supabase/templates/magic_link.html`, using `{{ .Token }}` instead of the default `{{ .ConfirmationURL }}` link — so verification always completes by typing the code back into the same tab, never a link that could open a different browser context) is applied to the **hosted** project only via that same Management API call, not via `supabase config push` — doing it through `config.toml` would force local dev to also route through Resend instead of the free, instant, fully-scriptable local Mailpit catcher (`http://127.0.0.1:54324`, has a REST API used to fetch the OTP code programmatically in tests). Local and hosted are intentionally slightly divergent here: same template content, different delivery mechanism.
    - Verified end-to-end, fully automated: local via Playwright + Mailpit's API (request code → fetch the email → extract the code → verify → confirm the created-while-signed-in lobby shows up in `/lobbies` → sign out reverts to the signed-out prompt). Hosted was verified structurally (config applied correctly, `signInWithOtp` accepted) — actually receiving/reading the emailed code on the live site needs a real inbox, so that last step is on whoever tests the deployed site.
11. **Polish/deploy groundwork**: EAS build profiles, web hosting env config, `.well-known` files for future universal links, CI (turbo remote cache, typecheck/lint — the check-types/lint/build pipeline itself is ✅ *done and green* across all four packages). *Not started.*
12. **User profiles (username + first/last name)**: ✅ *done*. Extends the `profiles` table that already existed (auto-populated by the `handle_new_auth_user` trigger on every `auth.users` insert) with `username` (unique, `^[a-z0-9_]{3,20}$`, lowercase-normalized), `first_name`, `last_name` — no new table. All three fields are independently optional and never gate anything: a small "Edit profile" link in the `Header` opens a `ProfileModal`, never an auto-popup. Reads go through the existing `profiles_select_self` RLS policy directly (`useProfile`); writes go through a new SECURITY DEFINER RPC, `rpc_update_profile`, because checking "is this username taken by someone else" requires reading across other users' rows, which RLS deliberately blocks for plain clients (same reasoning as `rpc_join_lobby`/`rpc_cast_vote`) — this also means no new RLS update policy or table UPDATE grant was needed, only `execute` on the function. Raises `INVALID_USERNAME`/`USERNAME_TAKEN` as plain exception messages, mapped to friendly text client-side. New `packages/shared`: `useProfile`, `useUpdateProfile`, `mapProfileRow`. New `apps/web`: `_components/ProfileModal.tsx`. Verified end-to-end via Playwright (format validation, save + Header update, persistence across reload, cross-user uniqueness conflict, then a distinct successful save).
13. **Wide dashboard layout for creator-facing pages**: ✅ *done*. `/create`, `/lobbies`, and `/lobby/[code]/manage` widened from `max-w-md` to `max-w-5xl`/`max-w-6xl` and reflow into a responsive grid at the `lg:` breakpoint (two-column form on `/create`, two-column QR+results on `/lobby/[code]/manage`, a real `<table>` on `/lobbies` alongside the original stacked-card markup, toggled via `hidden lg:block` / `lg:hidden` pairs) — below `lg:` every page is byte-for-byte the same single-column layout as before. Deliberately scoped to creator-facing pages only: `/`, `/login`, and the public `/vote/[code]` ballot screen stay focused centered cards, since they're single-task screens (a voter scans a QR code into `/vote/[code]` far more often on a phone than a desktop). No sidebar/app-shell — `Header` was already edge-to-edge and needed no change.
14. **10-lobby cap for signed-in creators**: ✅ *done*. `rpc_create_lobby` (still `security invoker` — the caller already has read access to their own `profiles` and `lobbies` rows) now raises `LOBBY_LIMIT_REACHED` if the caller has a real account (`not profiles.is_anonymous`) and already has 10+ lobbies. Anonymous creators are deliberately exempt — their lobbies self-delete after 7 days (step 15), so they don't need a separate cap. `/lobbies` shows "`n`/10 lobbies" and disables "+ New lobby" at the cap; `/create` maps the exception to a friendly message. Verified directly via SQL: the 11th `rpc_create_lobby` call for a 10-lobby signed-in test account raises `LOBBY_LIMIT_REACHED`; the same for an anonymous test account with 10 existing lobbies succeeds.
15. **7-day auto-delete of anonymous lobbies**: ✅ *done*. A `pg_cron` job (`select cron.schedule('delete-stale-anonymous-lobbies', '0 3 * * *', ...)`, migration `20260723054859_anonymous_lobby_cleanup.sql`) runs daily and deletes any lobby whose creator's `profiles.is_anonymous` is true and whose `created_at` is more than 7 days old. `options`/`participants`/`votes` need no extra handling — all three already `on delete cascade` from `lobbies` (init migration). Lobbies created by a signed-in account are never touched by this job, regardless of age. Entirely server-side (plain SQL scheduled in Postgres) — no Edge Function, no new client-facing RLS/grants. Verified by manually backdating one anonymous-owned and one signed-in-owned test lobby to 8 days old, running the job's exact `DELETE` body directly: the anonymous lobby (and a dependent `options` row) was removed, the signed-in-owned lobby survived untouched.
16. **Manual lobby delete + table/grid view toggle**: ✅ *done*. New RLS policy `lobbies_delete_own` (`auth.uid() = creator_id`) + `grant delete on public.lobbies to authenticated` (migration `20260723081544_lobby_delete.sql`) — no RPC needed, delete has no extra business logic beyond ownership, which RLS already expresses fully; `useDeleteLobby` is a plain client `.delete()`. A trash icon on every `/lobbies` row/card and a "Delete lobby" link on `/lobby/[code]/manage` both go through a shared `ConfirmDialog` component (`apps/web/app/_components/ConfirmDialog.tsx`) before calling it — deletion is permanent and cascades away options/participants/votes, so it's never a single accidental click. `/lobbies` also gained a table/grid view toggle (segmented control next to "+ New lobby", `lg:` and up only) persisted in `localStorage` (`votero:lobbies-view`) — below `lg:` the layout is unchanged (always the single-column stacked cards). Verified end-to-end: deleting one of three lobbies via the table view's trash icon removes only that one (confirmed directly via SQL against the two survivors); deleting via the manage page's "Delete lobby" redirects to `/lobbies`; the grid view renders a responsive card grid and the view choice persists across a reload.
17. **Back navigation on the manage page**: ✅ *done*. `/lobby/[code]/manage` had no way back to `/lobbies` short of the browser back button — the `Header`'s "Votero" wordmark only goes to `/`. Added a small "← My Lobbies" link above the title (falls back to "← Home" for an anonymous creator, since they have no lobby history to go back to).
18. **Sharing the QR/link/code, a "danger zone" delete button, and join-by-code**: ✅ *done*. The manage page's QR card gained three actions — **Copy link**, **Copy code** (`navigator.clipboard.writeText`, 2s "Copied! ✓" confirmation), and **Share** (`navigator.share` where supported, e.g. mobile — opens the native share sheet; falls back to copying the link on browsers without it, e.g. desktop) — answering "how does the creator actually get this to people" beyond just the QR image itself. "Delete lobby" moved from a bare text link to a proper `Button variant="danger"` with a trash icon, separated from the primary open/close actions by a divider, so it reads as a distinct, deliberate destructive action rather than a stray link. The home page gained a collapsed-by-default "Have a code? Join a lobby" toggle revealing an inline 8-character code input + Join button — a fallback for anyone who can't scan. **Superseded by step 19 below** (promoted to a full "Join a lobby" button with a proper scan-or-type choice). `apps/web/app/page.tsx` switched from a fixed `h-[calc(100vh-4rem)]` to `min-h-[...]` so optional extra content can never be clipped on a very short viewport — the default state still fits with no scroll, matching the original no-scroll fix. Shared `TrashIcon` extracted to `apps/web/app/_components/icons.tsx` (now used by both `/lobbies` and the manage page). Verified end-to-end via Playwright: copy actions land the right text on the clipboard, and Share's fallback path runs without throwing.
19. **"Join a lobby" button with in-browser QR scanning**: ✅ *done*. The home page now shows two equally-sized actions — "Create a lobby" (primary/filled, unchanged) and "Join a lobby" (secondary/outlined) — replacing the old collapsed code-only toggle from step 18. Clicking "Join a lobby" opens `JoinLobbyModal` (`apps/web/app/_components/JoinLobbyModal.tsx`) with two choices: **Scan QR code** or **Enter code instead**. This is a genuine in-browser camera scanner (`navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' } })` → draws video frames to an offscreen `<canvas>` in a `requestAnimationFrame` loop → decodes with the `jsqr` library, ~3-4KB gzipped, zero dependencies, chosen over the native `BarcodeDetector` Web API specifically because that API still isn't supported in Safari) — distinct from the existing "someone scans your shared QR with their phone's own camera app" flow, which already worked with zero code. This covers the complementary case: someone already on the site (laptop webcam, or a phone browser tab) wanting to scan a QR shown on another screen without leaving `votero.com`. Decoded content is parsed as a URL and only navigated to if its pathname matches `/^\/vote\/[A-Z0-9]+$/i` (works regardless of which environment — local/hosted — generated the QR); anything else shows "That doesn't look like a Votero QR code." The "Scan QR code" option is hidden entirely when `navigator.mediaDevices?.getUserMedia` doesn't exist, and getUserMedia rejection (permission denied) shows a friendly fallback message with a "Use code instead" link — the modal never dead-ends. Verified end-to-end via Playwright: the code-entry path (join a freshly created lobby by typing its code), the full scan pipeline using Chromium's fake-camera device (confirms the video/canvas/jsQR wiring runs cleanly end-to-end — not a substitute for testing real QR decoding, which is `jsqr`'s well-established job, not custom code here), switching from scan to code mid-flow (exercises the camera-stream cleanup path), and the permission-denied fallback message (tested in a separate browser instance without Chromium's fake-permission-UI flag, so a real denial actually occurs).
20. **7-day-deletion notice for anonymous creators**: ✅ *done*. Anonymous creators had no visibility into step 15's auto-cleanup job — the manage page now shows a small conditional notice gated on `!isSignedIn`, computed from `lobby.createdAt + 7 days` (not from close time — a common misreading, since the job only looks at creation time). Signed-in creators never see it, since their lobbies are never auto-deleted. Copy was corrected after review: it does **not** say "sign in to keep it permanently," since that would be false — `useVerifyOtp` replaces the session entirely rather than upgrading it, so an already-created anonymous lobby can never be transferred to an account after the fact. It instead says signing in now won't save *this* lobby, and that signing in *before* creating the next one is what keeps it around. Verified via Playwright: visible for an anonymous creator's freshly created lobby, absent for a signed-in creator's.
21. **Verified voter identity for open-ballot lobbies + profile pictures**: ✅ *done*. In "open" ballot mode the creator sees who voted for what, but `participants.display_name` was a free-text field nobody ever filled in (the voter UI never collected it), so every entry just read generic "Voter" — useless for actually verifying anyone, and trivially fakeable even if populated. Fixed by requiring a real signed-in account to vote in open-mode lobbies at all:
    - **Server-side (authoritative)**: `rpc_join_lobby` now raises `SIGN_IN_REQUIRED` if `lobby.ballot_mode = 'open'` and the caller's `profiles.is_anonymous` is true (migration `20260723124706_open_ballot_signin_required.sql`). Anonymous-mode lobbies are completely unaffected — verified no behavior change there.
    - **Client-side**: `/vote/[code]` shows a "sign in to vote" gate instead of auto-joining when the lobby is open-mode and the visitor isn't signed in; `/login` now supports a `?redirect=` param (wrapped in `<Suspense>`, required by `useSearchParams` for static export) so signing in from that gate lands back on the same ballot instead of `/lobbies`.
    - **Real identity in the ballot list**: `rpc_get_ballot_detail` (already `security definer`, already creator-gated) now joins `profiles` and `auth.users` to return `firstName`/`lastName`/`username`/`email`/`avatarUrl` per entry instead of the old always-null `displayName` — strictly additive to an already-locked-down function, no new grants needed. Display priority: full name (with `@username` as a muted secondary line) if set, else `@username` alone, else email as a last resort.
    - **Profile pictures**: new `profiles.avatar_url` column, written through the existing `rpc_update_profile` (extended with a `p_avatar_url` param — added via `drop function` + recreate rather than `create or replace`, since appending a parameter changes the declared signature and `create or replace` would otherwise silently create a second overloaded function instead of replacing the original) rather than a plain client update, for the same uniqueness-bypass reason `rpc_update_profile` already existed. A new `avatars` Storage bucket + per-user-folder RLS policies (public read; write restricted to `avatars/{own-user-id}/...`) are created via SQL migration (`20260723124713_profile_avatar.sql`), not `config.toml`'s `[storage.buckets]`, which is local-CLI-only and never reaches the hosted project. New `Avatar` component (`apps/web/app/_components/Avatar.tsx`) renders the photo or a colored-initial fallback; used in the `Header`, `ProfileModal` (upload UI: file-type/2MB client-side checks, `useUploadAvatar` hook, cache-busted URL so a changed photo shows immediately), and the ballot list.
    - Verified end-to-end via Playwright: anonymous visitor to an open-mode lobby sees the gate (not the ballot); signs in and lands back on the same lobby via `redirect`, then votes normally; the same lobby in anonymous mode still auto-joins with zero gate; a direct `rpc_join_lobby` call as an anonymous session is still rejected server-side even bypassing the client; the ballot list shows the real voter's email (no name/username set) correctly attributed to their chosen option; profile picture upload → Header updates immediately → persists across reload.
22. **Time-based (scheduled) auto-close**: ✅ *done*. A creator can optionally set an "auto-close at" date/time on `/create`; a new `pg_cron` job (`auto-close-scheduled-lobbies`, migration `20260724143737_scheduled_auto_close.sql`) runs every minute (finer-grained than the daily anonymous-cleanup job, since a scheduled close needs to actually feel timely) and flips any lobby past its `closes_at` from `open` to `closed` — same `closed_at = now()` convention `rpc_cast_vote`'s cap-triggered auto-close already uses. A lazy check inside `rpc_join_lobby`/`rpc_cast_vote` was considered and skipped: both are read from the plain `lobbies` table on the client (not through an RPC), so the row needs to already be `closed` in the database for the UI to react regardless, and the existing Realtime subscription on `lobbies` already pushes the cron sweep's update to any open page instantly once it commits — no extra client-side plumbing needed. `rpc_create_lobby` gained an optional `p_closes_at` param (validated `> now()` server-side, `CLOSES_AT_MUST_BE_FUTURE` otherwise; added via `drop function` + recreate for the same overload-avoidance reason as the avatar migration) and `lobby_to_json` now includes `closesAt`. Both the manage page ("⏰ Auto-closes `<date>`") and the voter's `/vote/[code]` page ("⏰ Voting closes `<date>`", shown above the ballot while `status === 'open'`) display it whenever set and not yet closed — voters have no other way to know a lobby is time-limited, so this isn't creator-only information. Verified: setting a closes_at in the past is rejected client-side with a friendly message before ever hitting the server; a lobby created with a near-future closes_at correctly shows the label on both the manage page and the voter's ballot page; manually backdating `closes_at` and running the sweep's exact SQL flips it to `closed` and the manage page reflects that on reload, with the auto-close label correctly disappearing once actually closed.
23. **Present Mode**: ✅ *done*. A fullscreen, presentation-optimized view (`apps/web/app/lobby/[code]/present/page.tsx`) meant to be projected at a live event — big QR code, live/hidden tally per `tallyVisibility` (via a new `size="lg"` prop on `TallyBars`), live join count, all reusing existing hooks (`useLobby`, `useLobbyResults`, `useLobbyRealtime`) with **zero schema changes**. Deliberately never renders `ballotDetail` (who-voted-for-what) regardless of ballot mode, since a projected screen is by definition not private. `Header` hides itself entirely on this route (`usePathname().endsWith("/present")`) — site nav would just be clutter on a screen meant to be watched, not navigated. Linked from a "🖥️ Present Mode" button on the manage page (opens in a new tab), and works in all three lobby statuses: draft ("Scan to get ready — voting opens soon"), open (live results), closed (final tally, winner crowned). No sign-in gating — it's a read-only aggregate display, not a join/vote action.
    - **Real bug found and fixed while building this**: `lobbies_select_public` excluded `status = 'draft'` ("an unopened lobby's QR/link shouldn't leak its existence before the creator opens it" — original RLS comment), but the manage page already shows that same shareable QR/link/code for a draft lobby, and the voter UI's "hasn't opened yet — check back soon" message (`docs/TESTING.md` scenario 2) had always assumed non-creators could read a draft lobby too. Verified empirically with two genuinely separate anonymous sessions that this was **never actually true** — a non-creator got zero rows back, meaning that documented/tested voter-facing message was unreachable in practice. Fixed via migration `20260724163741_fix_draft_lobby_visibility.sql` (drops the `status <> 'draft'` exclusion) — no new exposure, since `rpc_join_lobby` already refuses to join a non-open lobby regardless of who can read the row. Re-verified with the same two-separate-session test that a stranger can now see a draft lobby's "hasn't opened yet" state, which Present Mode's pre-open screen depends on.
24. **Multi-question surveys**: ✅ *done*. The biggest schema change so far — a lobby now asks 1+ questions instead of exactly one. Existing single-question lobbies are just the common case of "a survey with one question," not a separate lobby "kind": `/create` always supports 1+ questions.
    - **Schema** (`20260724170359_multi_question_schema.sql`): new `questions` table (`id, lobby_id, title, position, unique(lobby_id, position)`) mirroring `options`' own shape. `options`/`votes` each gain a `question_id` (fk cascade to `questions`, inferred server-side, never client-supplied). `lobbies` gains `question_count` (denormalized at creation, enables an O(1) "has everyone finished everything" check). `participants.has_voted` is redefined from "cast a vote" to "answered every question" (`answered_count >= question_count`), backed by a new `answered_count` column. A one-time data migration gives every pre-existing lobby a single default `questions` row (`position 0`, `title` = the lobby's own title) and repoints its existing `options`/`votes` rows to it — verified with a dedicated backward-compatibility test (simulated legacy-shaped rows via direct `psql`, ran the exact backfill snippet, confirmed correct linkage) before ever touching real data.
    - **The real one-vote-per-lobby constraint had to be relaxed**: `votes` had `unique (lobby_id, participant_id)` — a hard DB-level guarantee that physically prevented more than one vote per person per lobby. Replaced with `unique (participant_id, question_id)`, the actual "one vote per question" guarantee multi-question voting needs.
    - **Real bug found and fixed while building this**: `options` had `unique (lobby_id, position)`, which assumed one option list per lobby with positions counting from 0. The instant a second question's first option also claimed position 0 under the same `lobby_id`, `rpc_create_lobby` failed with a duplicate-key error — caught immediately by the direct-RPC verification pass (a 3-question test survey failed to create at all). Fixed by rescoping the constraint to `unique (question_id, position)`, since positions now restart per question, not per lobby.
    - **RPCs**: `rpc_create_lobby` takes `p_questions jsonb` (`[{"title": "...", "options": [...]}, ...]`) instead of `p_options text[]` (added via `drop function` + recreate, same overload-avoidance reasoning as the avatar/lobby-cap migrations), validating both `AT_LEAST_ONE_QUESTION_REQUIRED` and (per-question) `AT_LEAST_TWO_OPTIONS_REQUIRED`. `rpc_join_lobby` keeps its signature but its JSON response now nests `questions: [{ id, title, position, options }, ...]` instead of a flat `options` array. `rpc_cast_vote` keeps its signature too (`question_id` is looked up server-side from the option row) — the old blanket `ALREADY_VOTED` check becomes a per-question `ALREADY_ANSWERED_QUESTION` check against the new unique constraint, and the cap-triggered auto-close arithmetic becomes `votes_count >= joined_count * question_count`. `rpc_get_ballot_detail`/`rpc_get_tally` (via the `lobby-results` Edge Function) both now return an array grouped per question (`[{ questionId, questionTitle, tally|entries: [...] }, ...]`) instead of one flat array — the Edge Function itself needed no code change since it just forwards whatever the RPC returns. `option_to_json` gained a `questionId` field it never had before (added via `create or replace`, same signature) to keep the JSON options returned by every RPC consistent with the `LobbyOption` domain type.
    - **v1 simplification, explicitly not resolved**: refreshing mid-survey doesn't resume at the next unanswered question — `/vote/[code]`'s stepper always starts at question 1 on mount, and if a cast-vote attempt comes back `ALREADY_ANSWERED_QUESTION` (only reachable this way), the client silently advances past it instead of showing an error. Verified via Playwright: answer question 1 of 2, reload (stepper restarts at question 1), resubmit the same answer, confirm it silently advances to question 2 with no error shown, then finish the survey normally.
    - **`apps/web`**: `/create` restructured from one Title + one Options list into Title (survey-level) + a repeating **Questions** section (each with its own title input and its own options list, "+ Add option"/"+ Add question"). `/vote/[code]` restructured into a one-question-at-a-time stepper ("Question X of Y", Next/Submit) — the confirmed UX choice over one long scrollable page. The manage page and Present Mode both loop over the grouped per-question results, rendering one `TallyBars` block (with the question's own title as a sub-heading, only shown when there's more than one question) per question — no carousel/pagination, questions just stack vertically. Tally visibility and ballot mode stay lobby-wide settings in v1 (no per-question override).
    - Verified end-to-end via direct RPC calls (3-question survey created/joined/voted with `voter_cap=1`, confirmed `answered_count`/`has_voted`/auto-close arithmetic all correct, confirmed the unique constraint genuinely rejects a duplicate vote on the same question with `ALREADY_ANSWERED_QUESTION`/409) and via Playwright (create a 2-question survey through the real form, vote through the stepper as a second session, confirm manage page + Present Mode both show correct per-question tallies and titles, confirm the refresh-mid-survey silent-skip behavior, and confirm a classic single-question lobby still looks and works identically — no stepper indicator, button still reads "Vote ✋" not "Submit ✋", manage page omits the now-redundant single-question sub-heading).
25. **Present Mode multi-question alignment fix + downloadable results**: ✅ *done*.
    - **Real bug found and fixed**: Present Mode's per-question sub-heading used `text-center`, which centers the heading text within the full-width results column — but `TallyBars`' rows are left-aligned with a fixed-width label column, so on a 2+ question survey the second/third question's heading visibly drifted away from its own options' left edge (first question happened to look fine only because it sat right under the page title). Fixed by left-aligning the heading and adding a subtle divider between questions (matching the manage page's existing per-question separator). Also hardened responsiveness while in there: the QR code now scales down on narrow viewports (`[&>svg]:w-full [&>svg]:h-auto` instead of a fixed pixel size) and long question titles `truncate` instead of overflowing — verified with Playwright screenshots at five viewport sizes from a 1920×1080 projector down to a 390px phone width, confirming zero horizontal overflow and consistent alignment at every size.
    - **Downloadable results**: a real gap this closes — anonymous lobbies auto-delete after 7 days (Build Order step 17), so an anonymous creator previously had no way to keep a permanent record of a poll's outcome. The manage page's results card now has "⬇️ CSV" and "🖼️ Image" buttons (shown whenever a tally is visible, live or closed) implemented in `apps/web/app/_components/downloadResults.ts` — plain client-side generation, no new dependency and no backend involvement: CSV is a hand-built string (`lobby` metadata rows, then one `question,option,votes` row per option) triggered via a `Blob` + `<a download>`; the image is hand-drawn onto an offscreen `<canvas>` (2x scale for crisp output) mirroring `TallyBars`' visual design — same fixed series-color order, same rounded bars, the 🏆 winner crown only once `closed`, question sub-headings only shown for 2+ questions — then exported via `canvas.toBlob` to a PNG download. Deliberately uses hardcoded light-theme hex values (matching `--background`/`--foreground`/`--foreground-muted`) rather than reading CSS custom properties, since a downloaded file is a static artifact that may be viewed or printed anywhere, not rendered inside the app's own theme context.
26. **Free-text question type**: ✅ *done*. A question can now be `'choice'` (today's behavior) or `'text'` — voters type a short answer instead of picking an option. Additive on top of multi-question surveys: a survey's questions can freely mix both types, no separate "survey kind."
    - **Schema** (`20260725205409_free_text_questions_schema.sql`): new `question_type` enum (`'choice' | 'text'`), `questions.type` (default `'choice'` — existing rows need zero backfill beyond the default). `votes` gains `response_text text` and `option_id` is relaxed to nullable, with a `check ((option_id is not null) <> (response_text is not null))` (exactly one set) and a `char_length(trim(response_text)) between 1 and 300` length check — 300 keeps responses short-phrase-sized, which matters for the frequency-grouped, font-size-scaled display looking reasonable.
    - **RPCs**: `rpc_create_lobby`'s Postgres signature is unchanged (still `p_questions jsonb`) — only the jsonb's internal shape gains an optional `"type"` per question, so this is `create or replace`, not a drop+recreate. `'choice'` questions validate/insert options exactly as before; `'text'` questions skip both. New `rpc_submit_text_response(p_lobby_id, p_question_id, p_response_text) returns public.lobbies` mirrors `rpc_cast_vote`'s structure closely (lobby lock, existing-vote check via the same unique constraint, `answered_count`/`has_voted`/auto-close bump) but is kept as its own function rather than overloading `rpc_cast_vote` with mutually-exclusive optional params — matches this codebase's one-function-one-job convention. (Build Order step 27 later replaced the original `ALREADY_ANSWERED_QUESTION`-on-duplicate behavior with an upsert, once "let voters go back and change an answer" became a requirement.) `rpc_get_tally` groups text responses by `lower(trim(response_text))`, returning `responses: [{ text, count }]` ordered by count desc instead of `tally` (v1 simplification: exact-match-after-normalization only — "Pizza" and "pizzas" don't merge). `rpc_get_ballot_detail` returns the raw, unnormalized `responseText` per entry for text questions (open-mode, creator-only — already shows individual identity, so no aggregation makes sense there).
    - **New Edge Function** `submit-text-response` mirrors `cast-vote/index.ts` exactly (including the same live-tally broadcast on `lobby:{id}:tally`), consistent with this app's one-Edge-Function-per-RPC-action convention. Needed its own `deno.json` import map and a `[functions.submit-text-response]` block in `config.toml` — a new function isn't auto-discovered by the local edge-runtime container without both (surfaced as a `BOOT_ERROR` / then a bare-specifier resolution error until both were added).
    - **`apps/web`**: `/create`'s repeating Questions section gained a per-question "📊 Choice" / "✍️ Free text" toggle — selecting "Free text" hides that question's options list entirely (only a title is required). `/vote/[code]`'s stepper branches per current question: `'choice'` renders the existing `RadioCard` list unchanged; `'text'` renders a 300-char `<textarea>` wired to a new `useSubmitTextResponse` hook (mirrors `useCastVote`; the text input was local component state from the start, unlike the choice-selection state at the time, which still lived in the shared `useBallotStore` — see Build Order step 27, which retired that store). New `TextResponseCloud` component (`apps/web/app/_components/TextResponseCloud.tsx`) renders grouped responses as chips sized by `count / maxCount` (a lightweight word-cloud effect via font-size scaling, not a true word-cloud packing layout — chosen specifically because it renders consistently across the manage page, Present Mode, *and* the downloadable PNG with one simple layout), reusing `TallyBars`' fixed series-color order for visual consistency. The manage page's "who said what" (open-mode only) shows the raw `responseText` for text questions instead of an option label.
    - **`downloadResults.ts`** gained a `layoutChips` helper: since canvas has no native text wrapping and the image's total height must be known before the real canvas is sized, it does a throwaway-context measurement pass first (mirroring `TextResponseCloud`'s frequency-based sizing) before drawing. CSV's column header changed from "Option" to "Answer" to read sensibly for both question types.
    - Verified via direct RPC calls (mixed choice+text survey, frequency grouping correct across differently-cased/whitespaced responses, `ALREADY_ANSWERED_QUESTION`/`RESPONSE_TEXT_REQUIRED` rejections) and Playwright (create a mixed survey, vote through the stepper hitting both question types, confirm the manage page/Present Mode/CSV/PNG all render both types correctly, confirm existing single- and multi-question choice-only regression suites still pass unchanged).
27. **Let voters go back and change an answer mid-survey**: ✅ *done*. A "← Back" button on `/vote/[code]`'s stepper lets a voter revisit an earlier question and change their mind before finishing the survey. Deliberately scoped to *before* finishing only — revising an answer after completing the whole survey was explicitly ruled out as a separate, bigger decision (some use cases want a locked-in final answer) and isn't built.
    - **RPCs** (`20260725220112_rpc_vote_upsert_allow_edits.sql`, both `create or replace`, signatures unchanged): `rpc_cast_vote` and `rpc_submit_text_response` now check for an existing vote row for that participant+question *before* the insert path — if one exists, `update` its `option_id`/`response_text` in place and return, touching neither `participants.answered_count`/`has_voted` nor `lobbies.votes_count`/auto-close (it isn't a new answer being counted, just a change to one already counted); otherwise fall through to the original insert-and-bump-counters path unchanged. The `ALREADY_ANSWERED_QUESTION` error this replaced is deleted entirely (removed from `supabase/functions/_shared/errors.ts` too) — nothing raises it anymore.
    - **Incidental simplification**: this also fixes the old refresh-mid-survey behavior for free — previously, resubmitting an already-answered question after a refresh hit `ALREADY_ANSWERED_QUESTION` and was silently discarded client-side (the original pre-refresh answer stood, even though the voter thought they'd just re-answered it). Now the same resubmission is a genuine, harmless edit. The stepper's `advanceAfterAlreadyAnswered` special-casing was deleted as dead code, not kept alongside the new behavior.
    - **`packages/shared`**: the `useBallotStore` Zustand store (`packages/shared/src/store/ballotStore.ts`) is retired — it only ever held one global "current selection," which can't represent "every question's answer at once" the way Back requires. Its only consumer, `/vote/[code]`, now keeps two local maps keyed by question ID (`choiceAnswers`, `textAnswers`) instead, so revisiting a question shows what was previously picked/typed rather than a blank slate. `useCastVote` no longer imports the store or calls a reset on success (it never had anything to reset).
    - **`apps/web/app/vote/[code]/page.tsx`**: the "← Back" button (secondary variant, disabled while a mutation is pending) renders next to Next/Submit whenever `questionIndex > 0` — it's a pure client-side navigation, no server call, since the previous answer is already saved and now shown pre-filled from local state. The button and the whole voting form are already scoped inside the `!showResults` branch, so there's no way to go back once the survey is finished — no extra guard needed for the "no editing after completion" boundary.
    - Verified via direct RPC calls (cast a vote for Q1, cast a *different* vote for Q1 as the same participant, confirm the tally reflects only the latest choice and `votes_count`/`answered_count` did not double-count; same for a text-response edit) and Playwright (vote Q1, Next, Back, confirm the original pick shows selected, change it, Next again, finish, confirm the final tally reflects the changed answer — plus a full re-run of the existing single-question, multi-question, and free-text end-to-end scripts to confirm no regressions).
28. **Fix "X of Y have voted" showing an impossible ratio on multi-question surveys**: ✅ *done*. Real bug, caught from a live screenshot: a 2-question survey with 2 participants who'd both finished showed "4 of 2 have voted" on Present Mode. Root cause: the `lobby-results` Edge Function's `progress.votesCast` was just `lobby.votes_count` — a raw count of every individual question-answer submitted (one row per question per participant), which made sense back when every lobby had exactly one question but silently became "total answers, not total voters" once multi-question surveys shipped (Build Order step 24) and was never revisited.
    - **Fix**: `LobbyProgress.votesCast` renamed to `completedCount` and redefined as "participants who've finished the *whole* survey" (`participants.has_voted`), not a raw vote-row tally — updated in `packages/types/src/domain.ts`, the three UI call sites (`/vote/[code]`, the manage page, Present Mode), and `lobby-results/index.ts`.
    - **Second bug found fixing the first one**: my first attempt had the Edge Function query `participants` directly with the admin (service_role) client — but `participants` deliberately has **no** `service_role` grant (see `20260721154753_grants.sql`'s own comment: every `participants`/`votes` access goes through either an authenticated-reachable RLS policy or a `SECURITY DEFINER` RPC, never a raw service-role grant). That call failed with a bare `42501` permission error surfaced as an unhelpfully empty `{"error":""}` 400 response — caught immediately by testing the exact reported scenario rather than trusting the type-check/build pass alone. Fixed with a new `rpc_count_completed_participants(p_lobby_id) returns integer` (`SECURITY DEFINER`, `service_role`-only grant), mirroring `rpc_get_tally`'s existing pattern instead of opening a new grant.
    - Verified by reproducing the exact reported scenario: a 2-question survey, two participants who both complete it, viewed on Present Mode by a genuinely separate (non-creator) session with hidden tally visibility — confirmed it now reads "2 of 2 have voted" instead of "4 of 2." Re-ran the full existing regression suite (single-question, multi-question, free-text, back-button) to confirm no regressions.
29. **Manage page: hide creator-only controls from non-creator viewers**: ✅ *done*. Surfaced by a user report of a raw `{"error":"FORBIDDEN"}` when clicking "Close voting" — root cause turned out to be that their browser's anonymous session no longer matched the lobby's `creator_id` (anonymous lobbies have no recovery path if that specific session is lost, by design — see the "no anonymous→permanent account upgrade path" decision above), but the manage page had never actually checked this client-side: it showed Open/Close/Delete to *any* visitor with the link and let the server's existing `auth.uid() === creator_id` check (`rpc_set_lobby_status`, `lobbies_delete_own` RLS policy) reject the click after the fact, surfacing the raw error code as-is.
    - **Fix**: `apps/web/app/lobby/[code]/manage/page.tsx` now computes `isCreator = user?.id === lobby.creatorId` (via the already-available `useAuthUser`) and gates the Open/Close/Delete controls behind it — a non-creator visitor instead sees a small explanatory notice ("only its creator can open/close voting or delete it") in their place. The read-only parts (QR/share, Present Mode link, join progress, tally) stay visible to anyone with the link, unchanged — this only tightens the client UI to match the authorization boundary the server already enforced, it doesn't change what's actually accessible to whom. Any remaining `FORBIDDEN` the server does return (e.g. a stale render, or a session that flips mid-session) is now mapped to a friendly sentence instead of the raw error code.
    - Verified via Playwright: the creator's own session sees the normal controls and no notice; a genuinely separate (non-creator) session loading the same manage link sees the notice, no Open/Close/Delete buttons, and still sees the read-only QR/share section. Full regression suite re-run to confirm no regressions.
30. **UX polish pass: friendly error fallbacks + input constraints, everywhere**: ✅ *done*. Prompted by two reports — the sign-in flow showing a bare, unhelpful (in one case effectively empty) error, and the 6-digit code input on `/login` accepting unlimited characters — which led to auditing every form/error-display in the web app for the same two failure modes rather than just patching the two reports.
    - **Raw/unmapped error text**: every `friendlyXError`-style helper across the app (`/create`, the manage page — pre-existing from earlier steps) had a gap where an *unmapped* error code fell through to `return message` unchanged, one step away from showing something raw or (if `message` was empty/undefined) nothing at all. All of them, plus two pages that had no friendly-error helper at all (`/login`, `/vote/[code]`), now have a safe generic fallback ("Something went wrong. Please try again.") for anything they don't explicitly recognize — `/login` additionally maps common Supabase auth error patterns (invalid email format, expired/invalid OTP token, rate limiting) to plain sentences. `/lobbies`' lobby-delete flow had no error handling at all (a failed delete silently did nothing) — `ConfirmDialog` gained an optional `error` prop, wired up there and left the manage page's existing (already-friendly) delete error as-is.
    - **Unconstrained inputs**: the 6-digit OTP code input (`/login`) had `inputMode="numeric"` (a mobile-keyboard hint only, not a real constraint) but no `maxLength` and no filtering, so a desktop user could paste/type past 6 characters or include letters — fixed with `maxLength={6}` plus stripping non-digits on change. Same audit found the lobby title, question titles, and option labels on `/create` had no `maxLength` despite a `char_length(...) between 1 and 200` check constraint server-side (would have failed at submit time with a raw Postgres constraint-violation message instead of being preventable by typing), and the voter cap input had no upper bound despite `voter_cap <= 10000` server-side — all four now capped client-side (`maxLength={200}` / `max={10000}` + a matching `canSubmit`/validation check for the cap, since numeric inputs don't strictly enforce `max` while typing the way `maxLength` does for text). `ProfileModal`'s first/last name inputs had no constraint at all (no server-side length limit exists for them either) — given a generous `maxLength={100}` purely as basic input hygiene. The OTP flow, the lobby-code entry (`JoinLobbyModal`, already correctly truncated to 8 chars), and the free-text vote response (`maxLength={300}`, from an earlier step) were all checked as part of the same sweep.
    - Verified via Playwright (OTP code capped at 6 digits with non-digits stripped, the friendly OTP error text renders correctly instead of raw/empty, title/question-title/option inputs capped at 200 chars, the Create button disables when voter cap exceeds 10,000 and re-enables once valid) plus a full re-run of the existing regression suite (single-question, multi-question, free-text, back-button, creator-gating) to confirm no regressions.
31. **`TextResponseCloud`: badge styling + text preview truncation**: ✅ *done*. The original version (Build Order step 26) rendered each grouped response as bare colored text sized by frequency — fine for a couple of one-word answers, but a response can be up to 300 characters, and showing one at full length, scaled up to the largest frequency-driven font size, didn't read as a "collective thoughts" cloud at a glance — especially on Present Mode, viewed from across a room.
    - **Fix**: each response now renders as a rounded pill/badge (a light tint of its series color as the background, the solid series color as text — same fixed hue order as `TallyBars`, via new `--series-*-bg` CSS vars alongside the existing `--series-*` ones) instead of bare text, and long responses are truncated to a short preview (24–36 characters depending on `size`, with `…`) — the full, untruncated text is still available via the badge's hover tooltip, and always available in full in the CSV export regardless. `downloadResults.ts`'s canvas-drawn `layoutChips` was updated to match exactly (rounded-rect badges behind the text, same truncation), so the manage page, Present Mode, and the downloadable PNG all show the same look — the whole reason "grouped by frequency" was chosen over a true word-cloud layout in the first place.
    - Verified via Playwright (a very long response is truncated with an ellipsis and never shown in full on any of the three surfaces, a repeated short response still shows its "×N" count) plus a full re-run of the existing regression suite to confirm no regressions.
32. **Design pass: remove "AI slop" tells (emoji-as-icons + gradient-blob backgrounds)**: ✅ *done*. Prompted by explicit user feedback that the UI "feels like AI slop." An audit of every `.tsx`/`.ts` file under `apps/web/app` found two specific, fixable patterns rather than anything wrong with the color palette or typography: (1) emoji used as functional icons/labels (26 occurrences across 8 files, plus a "✓" typographic checkmark on `RadioCard`) — emoji render inconsistently across OS/browsers and carry no shared visual weight; (2) an identical blurred-gradient-circle decoration (`blur-3xl` + `bg-{brand|accent}-*/opacity`) repeated on 9 divs across 7 pages regardless of page content — applying the same decoration everywhere, not the blob look itself, was the actual tell.
    - **Fix**: adopted `lucide-react` (MIT, tree-shakeable) as the app's single icon system, replacing every emoji-as-icon and the three hand-rolled icons (`TrashIcon`, inline `TableIcon`/`GridIcon`) — `apps/web/app/_components/icons.tsx` deleted entirely once nothing imported from it. Icon sizing convention: `size={14}`–`{16}` for inline/button-prefix icons (matching the old 16×16 hand-rolled footprint), `size={40}` with a thinner `strokeWidth={1.5}` for empty-state "hero" icons. `vote/[code]/page.tsx`'s `EmptyState` component's `icon` prop changed from a rendered emoji string to a `LucideIcon` component reference. The one non-React rendering path — `downloadResults.ts`'s canvas-drawn PNG export — couldn't use a React icon component for its winner-trophy emoji, so a small filled 5-point star polygon is hand-drawn via trigonometry instead. All 9 gradient-blob divs were deleted outright (flat `--background` plus existing card/border treatment carries visual interest instead); each page's wrapping `relative`/`overflow-hidden` classes were left in place. Deliberately left untouched: the `→`/`←` arrow glyphs in button copy (standard UI convention, not an AI-slop tell) and the real `canvas-confetti` celebratory burst (a genuine delight moment, not a static glyph).
    - Verified via Playwright screenshots of every touched page (home, login, create, lobbies, manage in both draft/open states, present, vote at every state including the not-found empty state, and the join-lobby modal) confirming icons render at consistent size/weight with no layout shift, plus a full re-run of the existing regression suite (single-question, multi-question, free-text, back-button, creator-gating, progress-count, UX-fixes, badge-cloud, refresh-mid-survey) to confirm no regressions — three of those scripts needed their own selectors updated from `text=✍️ Free text` to `text=Free text` since the emoji they were matching on no longer exists, not because of an app regression.
33. **`TextResponseCloud`: fixed compact badge size + click-to-expand**: ✅ *done*. Follow-up to Build Order step 31 — user feedback was that the frequency-scaled badges (up to 2.25rem font on Present Mode) had gotten too large, closer to full sentences than a "preview" cloud, and the hover tooltip for the full text didn't work at all on touch devices or for an audience watching a projected screen with no mouse.
    - **Fix**: badges are now a fixed small size regardless of response frequency (the "×N" count already communicates frequency on its own, making the font-size scaling redundant on top of it) and truncate more aggressively (16/20 characters vs. the old 36/24). Each badge is now a real `<button>` — clicking it opens a small centered dialog (styled like the existing `JoinLobbyModal`) showing the full, untruncated text and the response count; clicking the × or the backdrop closes it. `downloadResults.ts`'s canvas-drawn `layoutChips` was updated to match (one fixed font size instead of a min/max scale) — the PNG export has no click interaction, so it keeps the same short preview text as its only way of showing the response.
    - Verified via Playwright (badges render at the new small fixed size regardless of a response's count, clicking a truncated badge shows the full text in a dialog, clicking its close button dismisses it) plus a full re-run of the existing regression suite to confirm no regressions.
34. **Manual light/dark theme toggle**: ✅ *done*. The app always had dark-mode styles (`dark:` Tailwind utilities, CSS custom properties with a dark override), but they only ever responded to the OS-level `prefers-color-scheme` media query — there was no in-app control, so a user whose OS was set to light had no way to see or use the app's dark mode at all, and vice versa.
    - **Fix**: `tailwind.config.cjs`'s `darkMode` changed from `"media"` to `["selector", '[data-theme="dark"]']` — Tailwind's `dark:` utilities and `globals.css`'s custom-property overrides now both key off a single `data-theme` attribute on `<html>` instead of the media query directly. A `next/script` with `strategy="beforeInteractive"` in the root layout sets that attribute before hydration/first paint (avoiding a flash of the wrong theme) — reading a stored choice from `localStorage` if the user has ever toggled, falling back to `prefers-color-scheme` otherwise, so the manual override sits on top of the OS default rather than replacing it. A new `ThemeToggle` component (a sun/moon `lucide-react` icon button in the header, hidden on Present Mode along with the rest of the header) flips the attribute and persists the choice. The storage-key constant lives in a plain (non-`"use client"`) module (`_components/theme.ts`) specifically because a Server Component importing a value from a `"use client"` module doesn't get the literal value across that boundary — the bootstrap script silently evaluated `localStorage.getItem(undefined)` when the constant was co-located inside `ThemeToggle.tsx`, a real bug caught during verification (see below). `<html>` also gained `suppressHydrationWarning`, since the pre-hydration script setting `data-theme` before React hydrates otherwise logs a harmless-but-noisy attribute-mismatch warning on every load (the same fix the `next-themes` library documents for the identical situation).
    - Verified via Playwright: clicking the toggle flips the whole page's colors and the icon immediately; the choice persists across a reload (this initially failed — `data-theme` silently reset to the system default after reload — which is how the `undefined`-key bug above was caught and fixed); a fresh context with no prior visit starts from the OS preference. Full regression suite re-run to confirm no regressions.
35. **Creator dashboard**: ✅ *done*. Signed-in creators previously landed on `/lobbies` (a bare list/table) after signing in, with no aggregate view of their activity across lobbies.
    - **Fix**: new `/dashboard` page, and the default post-sign-in redirect (`/login`'s `redirectTo`, when no explicit `?redirect=` param is present — e.g. the mid-vote sign-in gate still redirects back to the ballot as before) changed from `/lobbies` to `/dashboard`. No new backend/RPC work was needed: `useMyLobbies` already does `select("*")` on the creator's own `lobbies` rows, and the `Lobby` domain type already carries `joinedCount`/`votesCount` per lobby (needed for the manage page's join-progress bar and vote counts), so every stat is a client-side reduction over data already being fetched. The dashboard shows three stat tiles (lobbies created, total voters joined, total votes cast), a Draft/Open/Closed status breakdown, and a "Voters by lobby" ranked horizontal-bar chart (top 5 lobbies by `joinedCount`) — a single brand hue, not `TallyBars`/`TextResponseCloud`'s fixed multi-hue categorical palette, since this compares one metric across arbitrary named lobbies rather than a fixed set of recurring option/response series that need simultaneous color-coded comparison. The header gained a "Dashboard" link alongside the existing "My Lobbies" one.
    - Verified via Playwright: a freshly signed-in user with zero lobbies sees a "create one to start seeing stats" prompt rather than empty stat cards; after creating and opening a lobby and having a second session join and vote, `/dashboard` reflects the real counts and the new lobby appears in the bar chart; sign-in redirects to `/dashboard` rather than `/lobbies`. Full regression suite re-run to confirm no regressions.
36. **Mobile header overflow (signed-in) + disabled-button contrast in light mode**: ✅ *done*. Two user-reported bugs. (1) The signed-in header's nav (Build Order steps 34–35 both added items to it: Dashboard, My Lobbies, avatar+email, Edit profile, Sign out, plus the theme toggle) had no mobile treatment at all — on a narrow viewport it all wrapped across multiple cramped, overlapping lines. (2) `Button`'s `primary`/`danger` variants only changed their disabled *background*, leaving the base variant's text color (`text-white` / `text-red-600`) in place — `text-white` on `disabled:bg-neutral-300` and `text-red-200` on the `danger` variant's white background are both very low-contrast in light mode specifically (dark mode's disabled backgrounds happened to still contrast enough with white text, which is likely why this went unnoticed until the user compared both modes side by side).
    - **Fix**: on `sm` and up the header nav is unchanged; below `sm`, the signed-in nav collapses into a single avatar+hamburger trigger that opens a dropdown panel (identity label, Dashboard, My Lobbies, Edit profile, Sign out), closed by a click on its backdrop or any item inside it. `Button`'s disabled state now sets its own text color per variant (`disabled:text-neutral-400`/`dark:disabled:text-neutral-500` for `primary`, `disabled:text-neutral-300`/`dark:disabled:text-neutral-600` for `danger`) instead of inheriting the enabled-state text color.
    - Verified via Playwright at a 375px-wide viewport: the signed-in header no longer wraps/overlaps, the hamburger menu opens showing all items, closes on a backdrop click, and clicking a link inside it both navigates and closes the menu; the disabled "Create lobby" button is now clearly readable in light mode (previously near-invisible white-on-light-gray). Full regression suite re-run to confirm no regressions.
37. **Design audit + incremental fixes: home hero symmetry, H1 scale, macro spacing, one squared element**: ✅ *done*. Requested as a formal senior-designer audit against a 60-30-10 palette / 2-font / 8px-grid brief. A full read of `tailwind.config.cjs`, `globals.css`, and every page/component found the palette and font count already close to compliant (rose + gold on neutrals; Baloo 2 + Geist + a narrowly-scoped Geist Mono for the lobby code) — the real gaps were the home hero's textbook centered-symmetry composition, a page title (H1) rendered at five different sizes across pages for the same role, and a handful of macro spacing values (`px-5`/`py-3.5`/`p-3.5`) that had drifted off an 8px-multiple grid. The audit doc argued *against* forcing a literal 60/30/10 split (the app's actual ~80/16/6 neutral-dominant ratio is the correct call for a functional product UI, not a violation) and against a blanket spacing rewrite (most fractional values are icon-label micro-gaps that would look worse forced onto 8px steps) — both deliberately left alone.
    - **Fix, in priority order**: (1) `page.tsx`'s hero rebuilt as an asymmetric two-column layout on `lg:` and up — left-aligned copy/CTAs beside a tilted (`-rotate-2`, `hover:rotate-0`) demo card built from real product pieces (`QRCodeSVG` + `TallyBars` with static demo data), not stock/decorative art; mobile stays a single simple centered column with no card. (2) `lobby/[code]/manage` and `vote/[code]`'s page-title `<h1>` raised from `text-2xl` (24px) to `text-3xl` (30px), matching create/dashboard/lobbies/sign-in — modal titles (`text-xl`/20px) were already consistent and untouched. (3) The three concrete macro-spacing violators named in the audit fixed onto clean 8px steps: `Button`'s `px-5`→`px-4` (its `py-2.5` was kept — paired with `text-sm`'s line-height it already yields a clean 40px total button height); the home hero's own larger CTAs bumped to `px-8 py-4` to read as more prominent than standard in-page buttons; `lobbies/page.tsx`'s table cells `px-5 py-3`/`py-3.5`→`px-4 py-4`; Present Mode's code badge `px-5 py-1.5`→`px-6 py-2`; the free-text `<textarea>` and `RadioCard`'s `p-3.5`→`p-4`. Fractional icon-gap spacing (`gap-1.5`, `mt-0.5`, etc.) was deliberately left as-is. (4) `lobbies/page.tsx`'s table wrapper radius dropped from `rounded-3xl` to `rounded-lg` — one deliberately squared-off element, since the rest of the app has zero hard corners anywhere (37 `rounded-full`, 19 `rounded-3xl`, 12 `rounded-2xl`) and a data table reads as more considered next to something crisp than uniform softness everywhere.
    - Verified via Playwright across desktop/mobile and light/dark for the new hero, both resized H1s, the retiled table, and the demo card's real QR/tally content rendering correctly in both themes; full regression suite re-run to confirm no regressions.
38. **Input/card border visibility in light mode**: ✅ *done*. User-reported: form inputs and their surrounding cards on `/create` were hard to see in light mode — `border-neutral-200` (a very light gray, near-imperceptible against a white card) was the resting border for every input, button, and card, and text inputs additionally had zero background contrast against the white card they sit inside (`bg-[var(--surface)]` on both), leaving the border as the *only* visual cue for where a field was.
    - **Fix**: a new `--input-bg` custom property (`globals.css`) gives text inputs/textareas a subtly recessed fill distinct from the surrounding card's `--surface` in both themes (`#f7f3f1` light, `#2c2136` dark) — `inputClasses` (`_components/styles.ts`) and the one duplicated free-text `<textarea>` (`vote/[code]/page.tsx`) now use it instead of `--surface`. Every plain resting `border-neutral-200` in light mode — cards, secondary buttons, modals, the home hero's demo card, RadioCard's unselected state, etc. (~14 files) — darkened to `border-neutral-300`, a noticeably more visible neutral without going as heavy as a dark mode-style border. Two `border-neutral-200` instances were deliberately left alone: a `/60`-opacity section divider on Present Mode (meant to be faint) and the `danger` button variant's *disabled*-state border (a disabled control should stay less prominent than its resting state, not equally visible).
    - Verified via Playwright screenshots of `/create` and `/login` in both light and dark mode — every input now shows a clearly visible field boundary in light mode; dark mode unaffected (its borders were never touched, and the new input fill is a bonus improvement there too). Full regression suite re-run to confirm no regressions.
39. **`JoinLobbyModal`: option title/description ran together**: ✅ *done*. User-reported, with a screenshot: the "Scan QR code"/"Enter code instead" option buttons showed their bold title and muted description running into each other on one line instead of stacking, because the title and description were sibling `<span>`s with no block-level wrapper forcing them onto separate lines.
    - **Fix**: wrapped each button's title + description pair in a `flex flex-col gap-0.5` span — same stacking pattern already used elsewhere in the app (e.g. `LobbyCard` on `/lobbies`).
    - Verified via Playwright screenshots in both light and dark mode — title and description now stack cleanly on separate lines. Full regression suite re-run to confirm no regressions.
40. **Bar/donut chart toggle for Present Mode + a new signed-in-only detailed stats page**: ✅ *done*. Two requests: Present Mode's results felt visually flat for a projected screen, and there was no way to see a choice question's results as anything but the existing bar chart, nor a place for a signed-in creator to get a more detailed breakdown than the manage page's inline results.
    - **Fix**: a new `DonutChart` component (`_components/DonutChart.tsx`) renders a ring chart via stacked `<circle>` elements with `stroke-dasharray`/`stroke-dashoffset` (not hand-authored arc-path data) — same fixed categorical hue order as `TallyBars`/`TextResponseCloud`, a center label (total votes, or the winner + their percentage once closed), and a direct-labeled legend (option, count, percentage) since angle comparison alone is harder to read than a bar's length. `TallyBars` gained an opt-in `showPercentage` prop (existing call sites on the manage/vote pages are unchanged — default `false`). A small `ChartViewToggle` (segmented control, same visual pattern as the `/lobbies` table/grid toggle) and a `TallyChart` dispatcher (picks `TallyBars`/`DonutChart`/`TextResponseCloud` per question by type + the current view) tie it together; the choice is persisted to `localStorage` (`votero:chart-view`) and shared between the two places that use it. Present Mode now shows this toggle (defaulting to bar) above its results, with percentages on. A new `/lobby/[code]/stats` page — reachable from the manage page's new "Detailed stats" link and from the Dashboard's "Voters by lobby" chart rows (previously linking to manage) — shows the same toggle plus two stat tiles (voters joined, votes cast, via a newly-extracted shared `StatCard` component also used by the Dashboard) and the CSV/Image export buttons. Deliberately scoped stricter than the manage page: access requires `isSignedIn && user.id === lobby.creatorId` (not just a matching anonymous session) — an anonymous creator or a signed-in non-creator sees a friendly explanatory message instead, and a signed-out visitor sees a sign-in prompt (`?redirect=` back to the stats page). The manage page's own inline Results section is untouched (still plain `TallyBars`, no toggle) — this was a deliberate scope decision to avoid changing an already-shipped, working view.
    - Verified via Playwright: the manage page shows "Detailed stats" only for a signed-in creator; a signed-out visitor and a signed-in non-creator to `/lobby/[code]/stats` each see the correct gated message; a signed-in creator sees stat tiles, the toggle, and both chart views rendering correctly (including a mixed choice+free-text survey, where exactly one toggle applies only to the choice question and the free-text question's badge cloud is unaffected); Present Mode's toggle and both chart views render correctly at `lg` size; both pages checked in light and dark mode. Full regression suite re-run to confirm no regressions.
41. **More entry points to the stats page**: ✅ *done*. Follow-up to step 40 — the only ways in were the manage page's link and the Dashboard's chart rows, but `/lobbies` (which lists every lobby, not just the ones with enough votes to show up in the Dashboard's top-5 chart) and Present Mode (which has no header/nav at all, so no path out to anything) had none.
    - **Fix**: `/lobbies` gained a "Stats" link next to "Manage →" in the table view, and a small bar-chart icon button next to the delete icon in the grid/mobile card view — both always reachable, since this whole page already requires being signed in to see any lobbies at all. Present Mode gained a small "Detailed stats" link next to its status pill, shown only when the viewer is a real signed-in creator (`isSignedIn && user.id === lobby.creatorId`, newly checked there via `useAuthUser` — previously this page had no auth-awareness at all) so a stranger glancing at a projected screen never sees a link that would just dead-end at a sign-in wall.
    - Verified via Playwright: the `/lobbies` table and grid views both navigate to `/lobby/[code]/stats`; Present Mode's link only appears for the signed-in creator and navigates correctly. Full regression suite re-run to confirm no regressions.
42. **UX audit pass: browser tab titles, an unconfirmed destructive action, a slow error path, and a mobile layout bug**: ✅ *done*. Requested as a fresh "use the app, find pain points" pass rather than a specific bug report — walked every core flow (create, vote, manage, present, stats, sign-in, error/edge states) at both desktop and mobile widths and fixed four real findings.
    - **No per-page browser tab titles**: every route showed the root layout's static "Votero," so a creator with several lobby tabs open (manage in one, present in another, vote in a third — a very plausible scenario for this app) couldn't tell them apart. Fixed with a new `useDocumentTitle(label)` hook (`_components/useDocumentTitle.ts`), wired into all 8 pages (static labels for `/create`/`/login`/`/lobbies`/`/dashboard`; the loaded lobby's title for manage/present/stats/vote, falling back to a generic label while loading). This needed two false starts to get right, both instructive: a plain `document.title = ...` write in a `useEffect` with a `[label]` dependency only stuck on pages with a single loading phase (create, login) — on `/lobbies`/`/dashboard` (two sequential async phases: auth, then a data fetch) Next's own App Router title reconciliation silently reverted it on a later render. Rendering a `<title>` element directly in JSX (React 19's cross-tree hoisting) didn't help either — `document.title` per spec resolves to the *first* `<title>` in tree order, and the root layout's static one is structurally always first. The fix that actually holds: keep the `document.title` write, but also attach a `MutationObserver` to the title element for as long as the page is mounted, re-asserting the desired value immediately if anything else changes it — correct regardless of when or how often Next's internal mechanism fires.
    - **"Close voting" had no confirmation**: a single click, with no way to undo (no "reopen" action exists anywhere), yet — unlike "Delete lobby" — skipped the `ConfirmDialog` pattern entirely, despite already being styled `variant="danger"` in the code (i.e. already understood to be a risky action, just never actually gated). Fixed by routing it through a `ConfirmDialog`, same as delete. Building this surfaced a real bug in `ConfirmDialog` itself: its confirm button's label and pending-label ("Delete" / "Deleting…") were hardcoded, not derived from `confirmLabel` for the pending state — meaning this new dialog would have shown a "Delete" button under a "Close voting?" heading. Added a `pendingLabel` prop (defaults to "Deleting…", so the two existing delete-lobby usages are unaffected) alongside the existing `confirmLabel`.
    - **Slow "not found" resolution app-wide**: `providers.tsx` constructed `new QueryClient()` with no default options, so TanStack Query's own default (3 retries, exponential backoff) applied to every query in the app — for a deterministic failure like a mistyped `/vote/<code>`, this meant ~7-10 seconds stuck on a loading spinner before the "Lobby not found" state ever appeared (this exact symptom had been observed and set aside as an unconfirmed dev-environment quirk during an earlier design pass — this UX audit tracked down and fixed the actual cause). Set `defaultOptions.queries.retry` to `1` globally — enough to smooth over one genuine transient network blip, without the multi-second cascade for a case retrying can't fix. Confirmed via Playwright: resolution time dropped from ~7-10s to ~1.7s.
    - **Present Mode mobile layout bug**: found by testing a real lobby title at a phone width — the title (`text-4xl`/`sm:text-5xl`, no smaller mobile step) wrapped across 3 lines, squeezing the status pill and the (also new, this session) "Detailed stats" link into a cramped row beside it. Fixed with a smaller mobile base size (`text-2xl sm:text-4xl lg:text-5xl`) and `flex-wrap justify-center` on the row so the pills drop to their own centered line under a long title instead of crowding it.
    - **Minor**: the manage page's raw vote URL (shown above the lobby-code pill) used `break-all`, which on narrow viewports split the lobby code itself across two lines (e.g. "SNAGR7D" / "P"). Changed to a single-line `truncate` with a `title` attribute for the full URL on hover — the URL is already fully available via the "Copy link" button and the code pill directly below it, so truncating it loses nothing.
    - Verified via Playwright throughout: all 8 pages' titles (including the two that needed the MutationObserver fix, re-tested after each iteration); the close-voting dialog appears, Cancel leaves the lobby open, and confirming actually closes it; the not-found timing improvement measured directly; Present Mode's mobile layout and the vote-URL truncation both re-screenshotted. Full regression suite re-run to confirm no regressions.
43. **Committed, automated end-to-end test suite**: ✅ *done*. Every regression check up to this point (the "full regression suite" re-run after nearly every change in this log) lived only as ad-hoc Playwright scripts in an ephemeral session scratchpad — not committed, not runnable by anyone else, not a safety net for future changes. `@playwright/test` is now a real `apps/web` dev dependency with a proper config (`apps/web/playwright.config.ts` — `webServer` auto-starts/reuses `pnpm dev`, `workers: 1` since most specs share local Supabase state and running spec files concurrently caused spurious timeouts, including plausibly hitting Supabase auth's own OTP rate limit).
    - **What's covered**: 21 tests across 12 spec files in `apps/web/e2e/` — the 9 scripts already referenced throughout this log as "the regression suite" (single/multi-question, free-text, back-button, creator-gating, progress-count, refresh-mid-survey, badge-cloud, UX-fixes), plus new coverage for the Dashboard (empty state, stat tiles, status breakdown, the voters-by-lobby chart's link) and this session's chart-toggle/stats-page and UX-audit fixes. A shared `e2e/helpers.ts` (OTP retrieval from Mailpit, the sign-in flow, lobby-code parsing) removes the duplication that existed across the old ad-hoc scripts. `pnpm test:e2e` (or `test:e2e:ui` for interactive debugging) from `apps/web`; see `docs/TESTING.md` for the mapping from spec file to manual-guide scenario and what's deliberately still manual-only (camera QR scanning needs a second physical device; pure visual/motion judgment calls don't fit a pass/fail assertion).
    - Verified by actually running the full suite twice — the first run (default Playwright parallelism) had 6 spurious failures that all disappeared once `workers: 1` was set, confirming they were resource contention rather than real bugs; the second run passed all 21. One genuine bug was in a test itself (an ambiguous `getByText("votes")` locator matching both a StatCard label and the donut chart's center label) — fixed with `exact: true`. `pnpm check-types`/`pnpm lint`/`pnpm build` all confirmed clean with the new `e2e/` directory in the tree (it's covered by the same broad tsconfig `include`, so it's type-checked as part of the normal pipeline too).
44. **Error monitoring (`@sentry/nextjs`)**: ✅ *done and active*. Before this, a broken flow in production was only discoverable by a user reporting it or someone manually tailing Vercel/Supabase logs.
    - **What's wired up**: `apps/web/instrumentation-client.ts` (browser), `apps/web/instrumentation.ts` (Next's own hook — `register()` initializes Sentry separately for the `nodejs`/`edge` runtimes via `process.env.NEXT_RUNTIME`, and exports `onRequestError = Sentry.captureRequestError` so server-rendering errors that only used to show up in logs get captured too), `apps/web/app/global-error.tsx` (catches errors that escape every other boundary, e.g. a crash in the root layout itself), and `next.config.js` wrapped with `withSentryConfig` (enables source-map upload once `SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN` are set — all three still optional/unset; error capture doesn't need them, only readable un-minified stack traces do). All of it keys off `NEXT_PUBLIC_SENTRY_DSN` — with it unset, every `Sentry.init()` call is a documented no-op, so this was safe to ship even before the DSN existed.
    - **The DSN is set locally** (`apps/web/.env.local`, gitignored) and confirmed live end-to-end: a real uncaught error was triggered in a browser session and Sentry's ingest API responded `200` for the resulting envelope POST. **Still needed**: add the same `NEXT_PUBLIC_SENTRY_DSN` value to the Vercel project's environment variables so production errors are captured too — that's the one remaining step, and it's a dashboard setting, not a code change.
    - Two version-specific fixes needed along the way, both caught by `pnpm check-types`/`pnpm lint` rather than guessed: `disableLogger`/`automaticVercelMonitors` moved under a nested `webpack: {...}` object in this SDK version (the flat top-level options are deprecated, not removed, but the SDK warns on every build); `instrumentation-client.ts` needs `export const onRouterTransitionStart = Sentry.captureRouterTransitionStart` or the SDK logs an "ACTION REQUIRED" warning about unfiled navigation instrumentation. `next.config.js` and the two new `SENTRY_*`/`NEXT_RUNTIME` env vars needed their own lint fixes too: `next.config.js` joined `postcss.config.cjs`/`tailwind.config.cjs` in the shared eslint config's ignore list (Node-execution-context tooling, not application source, needs `process` global), and the three non-`NEXT_PUBLIC_`-prefixed env vars got added to `turbo.json`'s `globalEnv` (Turborepo auto-recognizes `NEXT_PUBLIC_*` vars via framework inference, but flags anything else undeclared).
    - Verified: `pnpm check-types`/`pnpm lint`/`pnpm build` all clean; a fresh dev server start and a full page walkthrough (`/`, `/create`, `/login`) showed zero console/page errors from the new client-side instrumentation; the full 21-test e2e suite re-run clean with the new files in the tree; and, once the real DSN was added, a genuine uncaught error was triggered and confirmed reaching Sentry (`200` response from its envelope ingest endpoint).
45. **CI pipeline (GitHub Actions)**: ✅ *done*. Before this, the committed e2e suite (Build Order step 43) and lint/type-check/build all only ran if a human remembered to run them by hand before pushing — nothing enforced it.
    - **What's wired up**: `.github/workflows/ci.yml`, two independent jobs on every push/PR to `main`/`develop` — `checks` (`pnpm lint`, `pnpm check-types`, `pnpm build`) for fast feedback, and `e2e` (boots a real local Supabase stack via `supabase/setup-cli` + `supabase start` — applies every committed migration and starts Edge Functions/Mailpit, exactly like a contributor's local setup — then runs the full 21-test Playwright suite against it). A failed `e2e` run uploads the HTML report as a build artifact for diagnosis without a local repro.
    - The local-dev Supabase anon key written into the workflow's `apps/web/.env.local` is Supabase CLI's well-known default demo key (deterministic from `supabase/config.toml`'s default JWT secret, identical across any local project that hasn't overridden it) — not a secret, and the same value already used in every contributor's own gitignored `.env.local`. `NEXT_PUBLIC_SENTRY_DSN` is deliberately left unset in CI so test runs don't get reported to Sentry.
    - Deliberately not in this pass: Playwright-browser and Docker-layer caching (both are reasonable speedups once the pipeline's baseline runtime is known, not needed to land the core win of "regressions get caught automatically").
    - Running it for real against the actual PR surfaced two genuine, previously-uncaught bugs — both fixed, not worked around:
      1. `pnpm check-types` runs repo-wide, and this was the first time anything ever type-checked `apps/mobile` as part of a full pipeline. It has two files importing `.css`/`.module.css` — NativeWind's type package only adds `className` props to RN components, it never declared CSS/CSS-module ambient types, so `tsc` had nowhere to resolve those imports. Fixed with `declare module "*.css"` and a typed `declare module "*.module.css"` in `apps/mobile/nativewind-env.d.ts`.
      2. The `checks` job's `pnpm build` step failed on `useSupabaseClient must be used within a SupabaseProvider` during static prerendering. Root cause: `apps/web/app/providers.tsx` deliberately skips mounting `SupabaseProvider` when `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` are unset (so `pnpm dev` doesn't crash before a contributor's `.env.local` exists) — but several statically-prerendered pages call `useSupabaseClient()` unconditionally, and the `checks` job never set those vars (only `e2e`'s job did; Vercel's real builds always have them, which is why this never surfaced before). Fixed by writing the same fake demo Supabase credentials into `checks`' `.env.local` before the build step — `createSupabaseClient()` never makes a network call on construction, so no live Supabase instance is needed just to satisfy the provider at build time.
46. **Terms of Service and Privacy Policy pages**: ✅ *done*. Needed before real emails get collected from anyone outside the immediate circle currently beta-testing.
    - `apps/web/app/terms/page.tsx` and `apps/web/app/privacy/page.tsx`, sharing a `LegalPage`/`LegalSection` layout (`_components/LegalPage.tsx`). A new site-wide `Footer` (`_components/Footer.tsx`, hidden on Present Mode the same way `Header` already hides itself there) links to both from every page; `/login`'s email step — the actual point an email address gets collected — now shows a "By continuing, you agree to..." line linking to both as well.
    - Content is grounded in the app's actual behavior rather than generic boilerplate: email-OTP sign-in (no passwords), optional profile/avatar upload, lobby content's visibility to other participants and its CSV/image export, the QR scanner's camera access (processed entirely client-side, never uploaded), `localStorage` (not cookies) for the session token and UI preferences, Sentry error capture, the 7-day anonymous-lobby auto-delete and 10-lobby cap (Build Order steps throughout this log), and the three third-party processors involved (Supabase, Vercel, Sentry) — plus an honest note that account deletion is currently a manual request, not self-serve.
    - Entity name ("Votero"), contact email, and domain (`votero-web.vercel.app`, no live custom domain yet) were supplied by the user rather than invented; no specific governing-law/jurisdiction clause is included, a deliberate choice for an early beta rather than an oversight.

## Explicitly deferred (schema/architecture already accommodates, not built now)
- Private invite-by-username rosters (`lobby_visibility='private'`, additive `invited_by`/`invite_status` columns on `participants` later).
- Per-lobby OTP verification (`lobbies.otp_required` column already reserved; branch slots into `rpc_join_lobby`/`rpc_cast_vote` later).
- Embeddable third-party widget (`/vote/[code]` already self-contained).
- Shared `packages/ui` component library.

---

## Verification

- **Local Supabase**: `supabase start`, apply migrations, seed a test lobby; call each RPC directly via `supabase.rpc(...)` in the SQL editor/psql to confirm cap enforcement (simulate concurrent joins), one-vote enforcement, and auto-close firing exactly when the last cap'd participant votes. ✅ *done* — see Build Order step 2's notes for the bugs this surfaced.
- **Edge Functions**: `supabase functions serve`, hit `join-lobby`/`cast-vote`/`lobby-results` with `curl`/Postman using both an anonymous session token and the creator's token; confirm `rpc_get_ballot_detail` returns `FORBIDDEN` when `ballot_mode='anonymous'` or caller isn't the creator. ✅ *done*, both locally and against the hosted project (Build Order step 3).
- **Anonymous-mode leak check**: with a lobby in `ballot_mode='anonymous'`, attempt (as the creator, via direct `supabase-js` client) to `select * from votes` or subscribe to `postgres_changes` on `votes`/`participants` — confirm both are refused (no policy / not in publication), proving the leak is structurally blocked, not just hidden in the UI. ✅ *done* — also confirmed `rpc_get_ballot_detail` is `FORBIDDEN` for the *creator themselves* on an anonymous-mode lobby, not just for other callers.
- **Web**: run `apps/web` dev server, create a lobby, open `/vote/[code]` in a second private/incognito window (simulating a fresh anonymous scanner), vote, confirm the creator's manage page updates live (Postgres Changes) and, for a live-tally lobby, confirm the broadcast-driven tally updates without a page refresh. *Blocked on Build Order step 6–7 (no UI yet).*
- **Mobile**: run `apps/mobile` via Expo Go, repeat the voter flow, and confirm the same `packages/shared` hooks drive identical behavior against the same Supabase project. *Blocked on Build Order step 8 (no UI yet).*
